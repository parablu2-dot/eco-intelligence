name: daily-commodities-energy

on:
  schedule:
    - cron: "20 22 * * *" # 매일 07:20 KST
  workflow_dispatch: {}

jobs:
  crawl-distill:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - run: npm install

      - run: npm run crawl:commodities_energy

      - run: npm run distill:commodities_energy
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Commit results
        run: |
          git config user.name "eco-intelligence-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/
          git diff --cached --quiet || git commit -m "chore: daily commodities_energy crawl+distill $(date -u +%F)"
          git push
